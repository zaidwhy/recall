# Recall backend. Pinned to the versions verified on this machine on 2026-09-15 (Python 3.12+;
# Render builds from these via the Dockerfile). Refresh with:
#   pip freeze | grep -iE "^(fastapi|uvicorn|python-dotenv|websockets|pillow|google-genai|chromadb|numpy)=="
fastapi==0.138.1
uvicorn[standard]==0.49.0
python-dotenv==1.2.2
websockets==15.0.1
pillow==12.3.0

# Gemini (Flash vision + Live API)
google-genai==2.10.0

# Memory store. Ships a local ONNX embedding model (all-MiniLM-L6-v2) - embeddings are free + offline.
chromadb==1.5.9
numpy==2.5.0
