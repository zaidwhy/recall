# Handoff - 2026-06-18 00:00 IST

## State at handoff

All Weeks 1–6 complete. Backend and frontend build clean. Everything pushed to GitHub master.

---

## What happened this session

### Bug fixes
| Bug | Fix |
|-----|-----|
| `recalled` spotlight card crash | `tools.py` sends `location`/`scene_description`/`objects_visible`/`time_ago`; JSX was reading `location_label`/`description`/`objects`/`minutes_ago` → TypeError on `.map()`. Fixed all four field names + added `??[]` null guard on `objects_visible` |
| `video.play()` AbortError on camera start | React re-render replaces `srcObject` before play() resolves → harmless AbortError surfaced as a visible error. Now caught and silently ignored |
| `FLASH_MIN_GAP_S` NameError on startup | Startup log line referenced the constant before it was defined. Fixed by reordering: constants → `_last_flash_call` tracker → log line |
| Stale "gemini-1.5-flash" log message | Updated to use `perception.VISION_MODEL` dynamically |
| `global` declaration after first use | `_next_scan_at` used in `record_status` at top of `_ingest_loop`, `global` declared later in a nested `try` → SyntaxError. Moved `global` to top of function |

### Model crisis resolution
- `gemini-2.5-flash` - 20 RPD free tier (the only working option)
- `gemini-1.5-flash` - deprecated, 404
- `gemini-2.0-flash` - `limit: 0` on free tier (requires billing)
- Final: `gemini-2.5-flash` with `FLASH_MIN_GAP_S = 120` (2-min gap) and `FLASH_DAILY_BUDGET = 18`

### Rate limiting infrastructure
- `FLASH_MIN_GAP_S = 120` in both ingest loop and analyze handler
- `FLASH_DAILY_BUDGET = 18` - hard stop before hitting the real limit
- `_flash_calls_today` counter shared across handlers
- 429 backoff: parses retry-delay from error string, sleeps that long + 5s
- Flash call count + next_scan_at sent to frontend in every ingested/updated/record_status message

### New UX features (this session)

**Recording experience:**
- Auto-start recording - WS `onopen` sends `record_start` after 2s delay
- `scanning` WS event sent before every Flash call - frontend shows blue "Scanning…" pill + video sweep overlay
- Countdown timer - `nextScanAt` state, 1-second `setInterval`, shows `· 47s` in the pill
- Dynamic header tagline - updates to "scanning…" / "next scan in Xs" while recording
- Empty recording state - pulsing 👁 with guidance shown when `recording && timeline.length === 0 && !scanning`

**Timeline:**
- `fmtRelative(ts)` helper - "just now" / "3m ago" / "2h ago" / "Jun 17"
- Chip overflow - `entry.objects.slice(0, 4)` + "+N more" chip
- `@keyframes slide-in` - new cards animate in from above (0.28s ease-out)
- `document.title` effect - "Recall (3)" with live count

**Debug panel:**
- Added "Flash: 3/18 calls" and "Next scan: 47s" rows

**Backend `/health`:**
- Now returns `vision_model`, `flash_calls_today`, `flash_budget`, `next_scan_in_s`

---

## Files changed this session

| File | Changes |
|------|---------|
| `backend/main.py` | `_next_scan_at` var; `scanning` WS event; `next_scan_at` in all messages; `record_status` includes quota; `/health` quota fields; `global` moved to function top |
| `backend/perception.py` | `VISION_MODEL = "gemini-2.5-flash"` (final) |
| `frontend/src/App.jsx` | Full rewrite: auto-record, scanning state, countdown, fmtRelative, chip overflow, document.title, empty state, recalled field name fix, debug rows |
| `frontend/src/App.css` | `.pill-scanning`, `.scan-overlay`, `@keyframes scan-sweep`, `@keyframes slide-in` on `.memory-entry`, `.empty-recording`, `.chip-more` |
| `README.md` | Added "Features" section (4 subsections), updated quick start, project status Week 6 |
| `frontend/dist/` | Rebuilt |

---

## Current architecture

```
Phone (HTTPS via cloudflared tunnel)
  │
  ├─ video frames every 2s ──► last_frame.jpg
  │
  │  _ingest_loop (every 5s):
  │    has_scene_changed() → numpy pixel diff
  │    gap check (120s) + budget check (18/day)
  │    → send "scanning" to frontend
  │    analyze_frame() → gemini-2.5-flash
  │    log_observation() → ChromaDB
  │    → send "ingested"/"updated" + next_scan_at
  │
  ├─ mic audio → Gemini Live (gemini-3.1-flash-live-preview)
  │    recall_memory() tool → ChromaDB (time-decay re-rank)
  │    → "recalled" WS event → spotlight card
  │
  └─ WS messages: scanning / ingested / updated / recalled / transcript / record_status / error
```

---

## Test before next step

### Minimal smoke test (after quota resets at midnight)
1. Restart backend - first log line: `Vision model: gemini-2.5-flash  |  min gap: 120s`
2. Open tunnel URL on phone
3. Tap "Start camera" - recording should auto-start (no second tap)
4. Recording pill shows `REC · 0 scenes · ready`
5. Move camera to show a distinct scene, wait up to 2 minutes
6. Pill turns blue "Scanning…" → then shows `REC · 1 scene · 17 calls left`
7. Enable voice → ask "what's on the [location you recorded]?"
8. Spoken answer + recalled spotlight card appear

### Recalled card test (specifically tests the bug fix)
- The spotlight card should show: location name, description, object chips, time_ago
- If any of these show "undefined" the field-name fix didn't apply → check the build

---

## Known constraints

- **20 RPD hard limit** on `gemini-2.5-flash` free tier. Quota resets at midnight (Google's time).
- **`_flash_calls_today` resets on server restart** - Google's counter does not. If you restart the server mid-day, the on-screen count restarts at 0 but Google's real count continues.
- **Auto-record on every WS reconnect** - intentional (demo UX). If you stop recording manually and the tunnel reconnects, recording resumes.
- **Windows App Control** - was the blocker for running backend in Claude Code Bash. User turned it off. Now `python -m uvicorn` works from Bash.

---

## Next steps (priority order)

1. **Test** - smoke test above, especially the recalled card fix
2. **Demo scenario** - place 3–4 objects in distinct rooms (keys on kitchen counter, charger on desk, book on coffee table, glasses on bathroom sink). Record fresh. Ask Recall about each.
3. **Record 60s demo video** - screen record on laptop + phone camera view side by side, or just phone screen
4. **README: embed demo video** - once video URL exists, add it above the "How it works" section
5. **LinkedIn posts** - build-in-public series

---

## Backend startup

```powershell
python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000
```

Expected first log line:
```
Vision model: gemini-2.5-flash  |  min gap between Flash calls: 120s
```

Tunnel:
```powershell
cloudflared tunnel --url http://localhost:8000
```
