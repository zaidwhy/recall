---
title: Session Handoff
date: 2026-06-17T19:04:00
project: recall
---

# Session Handoff - 2026-06-17

## Summary
Week 3 was fully implemented (recall_memory tool wired into Gemini Live, confidence gate, temporal filter, spotlight card), then two bugs found during phone testing were fixed: voice session dropping after the first tool-call round-trip, and false negatives from a threshold that was too strict. The frontend was also unblocked from a Windows Application Control policy blocking Rollup's native binary.

---

## Work Completed

### Week 3 Core (new this session)
- **`backend/memory.py`** - added `distances` to `recall_memory` query, `RECALL_MAX_DISTANCE` constant, `recall_for_tool()` helper with confidence gate, `_unpack` extended with optional distances param
- **`backend/tools.py`** - stub replaced with full `RECALL_TOOL` declaration + `handle_tool_call()`, distance logging for calibration
- **`backend/live.py`** - `tools=[RECALL_TOOL]` in `LiveConnectConfig`, new SYSTEM prompt (photographic memory identity), tool-call dispatch via `asyncio.to_thread`, `recalled` WS message to phone, try/except around tool block
- **`frontend/src/App.jsx`** - `recalled` state, WS handler, spotlight card above timeline, cleared on `startTalk`
- **`frontend/src/App.css`** - `.recalled` spotlight (accent border, slide-in animation), `.recalled-badge`, `.recalled-x`

### Bug fixes (post phone test)
- **Voice auto-restart** - `_runner` in `main.py` now loops: when `run_live` exits (session.receive() exhausts after tool round-trip), drains stale queue signals and reconnects immediately
- **False negatives** - `RECALL_MAX_DISTANCE` raised 1.0 → 1.4; 1.0 required cosine similarity ≥ 0.5 which was too strict for question-vs-observation semantic matching
- **Session resilience** - tool call block wrapped in try/except so a bad `FunctionResponse` can't crash the receive loop

### Frontend build unblock
- Vite downgraded 5.4 → 4.5 in `frontend/package.json` - Vite 5 ships Rollup 4 which uses a native `.node` binary blocked by Windows Application Control in Claude Code's process context; Vite 4 uses Rollup 3 (pure JS)
- `package-lock.json` regenerated (only uncommitted change - should be committed)

### Docs
- `CHANGELOG.md` - Week 3 entry + bug fix addendum
- `handoffs/WEEK-3-COMPLETED.md` - created, updated with bug fixes and calibration notes

---

## Dead Ends - Do Not Retry

### Frontend build in Claude Code's Bash context
| Approach Tried | Why It Failed |
|----------------|---------------|
| `npm run build` with Vite 5 / Rollup 4 | Windows Application Control blocks `@rollup/rollup-win32-x64-msvc.node` with `ERR_DLOPEN_FAILED` |
| Clean reinstall then retry | Binary is re-downloaded identical; still blocked |
| Delete `@rollup/rollup-win32-x64-msvc` dir to force WASM fallback | Rollup 4's `native.js` throws `MODULE_NOT_FOUND` with same friendly error - no actual WASM fallback in this version |

**What to try instead**: Downgrade Vite to `^4.5.0` (already done). Rollup 3 = pure JS, no native binary, no policy issue.

### Running the backend from Claude Code's Bash context
| Approach Tried | Why It Failed |
|----------------|---------------|
| `python -m uvicorn backend.main:app ...` | Windows Application Control blocks `pydantic_core`'s compiled `.pyd` extension - same policy |

**What to try instead**: Always run the backend in the user's own PowerShell terminal. Claude Code can edit files and build the frontend (Vite 4); the backend must be started by the user.

### RECALL_MAX_DISTANCE = 1.0
| Approach Tried | Why It Failed |
|----------------|---------------|
| Default threshold from handoff plan | Cosine similarity threshold of 0.5 is too strict - question phrasing ("where is my charger") vs. scene description ("desk with electronic accessories") semantic gap pushes real hits above 1.0 |

**What to try instead**: 1.4 (current). Calibrate down toward 1.1 if false positives appear; watch server console `top_dist=` lines.

---

## Key Decisions

- **Vite 4 pinned** (`"vite": "^4.5.0"` in `package.json`): Trade-off - slightly older toolchain, but fully functional. Vite 5 blocked by Windows App Control on this machine.
- **Auto-restart in `_runner`, not in `run_live`**: The audio queue is shared across restarts; keeping it alive in `_runner` avoids reinitializing the WS message plumbing.
- **`asyncio.to_thread` for ChromaDB**: It's a synchronous blocking call; running it inline would stall audio delivery from the receive loop.
- **RECALL_MAX_DISTANCE = 1.4**: Just under √2 ≈ 1.414 (L2 distance for fully orthogonal unit vectors); accepts anything with any semantic overlap. Reduce toward 1.1 if wrong objects start being recalled.

---

## Pending Issues
- [ ] `RECALL_MAX_DISTANCE = 1.4` may need further calibration - **MED** - watch `top_dist=` in server logs during real use; update value in `memory.py`
- [ ] `frontend/package-lock.json` not committed - **LOW** - commit after confirming build works
- [ ] `week-3` git tag not yet created - **LOW** - `git tag -a week-3 -m "Week 3: recall tool + confidence gate + spotlight"` + push
- [ ] Voice still occasionally intermittent per user - **MED** - auto-restart helps but root cause (why `session.receive()` exhausts after one tool-call round-trip) not confirmed; may be Gemini Live API behavior with manual activity detection + tools

---

## Next Steps
1. Commit `frontend/package-lock.json`: `git add frontend/package-lock.json && git commit -m "pin Vite 4 / Rollup 3 (native binary blocked by App Control)"`
2. Tag week-3: `git tag -a week-3 -m "Week 3: recall tool + confidence gate + spotlight"` + `git push origin master --tags`
3. Monitor `top_dist=` values in server console; update `RECALL_MAX_DISTANCE` in `backend/memory.py` if needed
4. Start Week 4: `eval/benchmark.py` - staged object placements → scripted questions → recall@1/@3 + latency → printed in README

---

## Files Modified This Session
| File | Change |
|------|--------|
| `backend/memory.py` | distances in recall_memory, RECALL_MAX_DISTANCE (1.0→1.4), recall_for_tool() |
| `backend/tools.py` | Full implementation (was stub): RECALL_TOOL + handle_tool_call() + distance logging |
| `backend/live.py` | tools=[RECALL_TOOL], new SYSTEM prompt, tool-call dispatch, recalled WS msg, try/except |
| `backend/main.py` | _runner loops for auto-restart on session close |
| `frontend/src/App.jsx` | recalled state, WS handler, spotlight card, clear on startTalk |
| `frontend/src/App.css` | .recalled spotlight styles |
| `frontend/package.json` | vite ^5.4.0 → ^4.5.0 |
| `frontend/package-lock.json` | regenerated for Vite 4 (uncommitted) |
| `CHANGELOG.md` | Week 3 entry + bug fix addendum |
| `handoffs/WEEK-3-COMPLETED.md` | created + updated with bug fixes |

---

## Context for Future Sessions

- **Never enable billing on the Gemini project** - it deletes the free tier. All API calls are on the free tier.
- **Backend must run in user's own PowerShell** - Windows Application Control blocks compiled `.pyd`/`.dll`/`.node` extensions in Claude Code's process. Frontend builds work (Vite 4 is pure JS).
- **Live model**: `gemini-3.1-flash-live-preview` (hardcoded via env `GEMINI_LIVE_MODEL`). `gemini-2.5-flash-live` does not exist.
- **ChromaDB distance metric**: Default L2. `all-MiniLM-L6-v2` produces 384-dim unit vectors, so L2 ∈ [0, 2]; cosine hits ≈ 0.4–0.9 L2 for good matches.
- **Vite 4 is intentional** - do not upgrade to Vite 5 without verifying the native Rollup binary is unblocked.

---

## Resume Line
> "Load handoffs/HANDOFF-2026-06-17-1904.md and start Week 4 - build eval/benchmark.py: staged object placements → scripted questions → recall@1/@3 + latency, printed into README. Also commit package-lock.json and tag week-3 first."
