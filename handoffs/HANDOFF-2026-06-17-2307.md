# Handoff - 2026-06-17 23:07 IST

## State at handoff

All Weeks 1–4 complete, Week 5 UI complete. Backend and frontend build clean. Pushed to GitHub.

---

## What happened this session

### Gemini model crisis + resolution

| Attempt | Model | Outcome |
|---------|-------|---------|
| Original | `gemini-2.5-flash` | 20 RPD free-tier - exhausted in first session |
| Switch 1 | `gemini-1.5-flash` | 404 NOT_FOUND - model deprecated |
| Switch 2 | `gemini-2.0-flash` | `limit: 0` - no free-tier for `generateContent` (requires billing) |
| **Final** | **`gemini-2.5-flash`** | Only model with any free-tier vision access (20 RPD) |

**Free tier reality as of June 2026:** `gemini-2.5-flash` is the only Flash model that supports `generateContent` on a free (no-billing) API key. All others either 404 or have a 0-request free quota.

### Rate limiting fixes in main.py

- `FLASH_MIN_GAP_S = 120` - 2-minute hard floor between any Flash call (was 30s)
- `FLASH_DAILY_BUDGET = 18` - stops at 18/20 to keep 2 in reserve
- `_flash_calls_today` counter - increments in both ingest loop and analyze handler
- Budget exhaustion: ingest loop sleeps 5 min, analyze handler returns error message
- 429 backoff: parses retry delay from error message, sleeps that long
- Flash call count sent to frontend in `ingested`/`updated` WS messages

### Frontend: flash budget indicator

- `flashCalls` / `flashBudget` state (defaults to 0/18)
- Updated on every `ingested` or `updated` message from backend
- Recording pill shows `· N calls left` when `flashCalls > 0`
- Pill turns yellow when budget exhausted (`.budget-warn` class)

### Critical bug fixed: recalled spotlight card

`tools.py` sends renamed fields: `location`, `scene_description`, `objects_visible`, `time_ago`  
App.jsx was reading: `location_label`, `description`, `objects`, `minutes_ago`

`recalled.objects.map()` would throw TypeError on first voice query result, crashing the React render.

**Fixed**: App.jsx recalled section now uses the correct field names. Also added `??[]` null guard on `objects_visible`.

### Other fixes

- `video.play()` AbortError now caught and ignored (harmless race condition on camera start)
- Stale log message "gemini-1.5-flash quota" updated to use `perception.VISION_MODEL`
- NameError bug (startup log line before constants defined) - fixed in prior work
- `import time` added to `main.py` (was missing)
- Windows App Control turned OFF by user - can now run backend from Claude Code Bash

---

## Current architecture

```
Phone (HTTPS via cloudflared tunnel)
  │
  ├─ video frames (JPEG, every 2s) ──► backend/main.py ──► data/last_frame.jpg
  │                                         │
  │                                    _ingest_loop (every 5s)
  │                                         ├─ has_scene_changed() [numpy diff]
  │                                         ├─ gap check (120s min)
  │                                         ├─ budget check (18/day)
  │                                         └─ analyze_frame() → gemini-2.5-flash
  │                                              └─► log_observation() → ChromaDB
  │
  ├─ mic audio (PCM16 16kHz) ──────────► live.py ──► Gemini Live (gemini-3.1-flash-live-preview)
  │                                         └─ recall_memory tool → memory.recall_for_tool()
  │                                              └─ time-decay re-ranking (DECAY_WEIGHT=0.25)
  │
  └─ WS messages ◄────────────────────── ack / ingested / updated / recalled / transcript / error
```

---

## Files changed this session

| File | Change |
|------|--------|
| `backend/perception.py` | VISION_MODEL default = `gemini-2.5-flash` (final) |
| `backend/main.py` | FLASH_MIN_GAP_S=120, FLASH_DAILY_BUDGET=18, counter, budget guard in both handlers, flash_calls in WS messages |
| `frontend/src/App.jsx` | flashCalls/flashBudget state, budget pill, recalled field name fix, AbortError fix |
| `frontend/src/App.css` | `.flash-budget`, `.recording-pill.budget-warn` |
| `frontend/dist/` | Rebuilt |

---

## Test before next step

**Goal:** Record one real scene and ask one voice question. Should work within today's remaining quota.

1. Check how many Flash calls were made today - backend logs show `flash call #N/18`
2. If calls remain: restart backend, open tunnel URL, start camera, hit Record
3. Wait 2 minutes in one spot → recording pill should show "REC · 1 scene · N calls left"
4. Enable voice → ask "what's on the [location you recorded]?"
5. Recalled card should appear with location, description, object chips, time_ago

**If quota is exhausted for today:** Wait until midnight for reset. Quotas are per-day per-model per-project.

---

## Known constraints / gotchas

- **20 RPD is the hard limit.** At 120s minimum gap, a 40-minute session uses all 20 calls. For a real demo, start recording fresh the day of the demo.
- **Quota resets at midnight** (Google's timezone, likely Pacific). Server restart resets `_flash_calls_today` but not Google's counter.
- **Never enable billing** on this API key - it removes the free tier.
- **GEMINI_VISION_MODEL in .env** overrides the code. If you set it, set it to `gemini-2.5-flash`.
- **Recalled card** was broken (wrong field names). Now fixed - the voice Q&A flow was silently crashing the recalled section.

---

## Next steps (priority order)

1. **Test** - record one scene, ask one voice question, verify recalled card appears correctly
2. **Demo scenario** - place 3-4 objects in different rooms, record all, verify Recall answers for each
3. **Record 60s demo video** for README / LinkedIn
4. **README final pass** - architecture diagram, demo video embed, "why not another AI wrapper" section
5. **LinkedIn posts** - build-in-public content

---

## Backend startup checklist

```powershell
# In PowerShell from C:\Users\Asus\projects\recall\
python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000
```

First log line should read:
```
Vision model: gemini-2.5-flash  |  min gap between Flash calls: 120s
```

If it says anything other than `gemini-2.5-flash`, check `.env` for `GEMINI_VISION_MODEL=` override.
